steps:
 - script: |
     cargo run -- --suites suites --config ci\config\basic.json
   displayName: Basic Run
 - script: |
     cargo run -- --suites suites --config ci\config\debug.json
   displayName: Debug Run
 - script: |
     cargo run -- --suites suites --config ci\config\optimize.json
   displayName: Optimized Run